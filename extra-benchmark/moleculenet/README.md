# MoleculeNet benchmark — reserved

This directory is intentionally not initiated yet. It will hold the separate
frozen-feature evaluation on BACE, BBBP, ESOL, FreeSolv, and Lipophilicity.
The preceding locked internal test-partition representation benchmark is now
complete and audited.

No MoleculeNet endpoint benchmark has been launched. Its split identities,
models, hyperparameter selection, metrics, and timing protocol must be frozen
before comparator execution.
